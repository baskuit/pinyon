#include <surskit.hh>

int main () {

    // prng device{};
    // uint64_t seed = device.uniform_64();
    // std::cout << "seed: " << seed << std::endl;

    // using RationalState = RandomTree<RandomTreeRationalTypes>;
    // using RationalModel = MonteCarloModel<RationalState>;
    // RationalState rational_state{seed, 1, 2, 2, 1, 0};
    // RationalModel rational_model{0};
    // TraversedState<RationalModel> rational_solved{rational_state, rational_model};
    // std::cout << "matrix:" << std::endl;
    // rational_solved.current_node->stats.nash_payoff_matrix.print();
    // std::cout << "strats:" << std::endl;
    // math::print(rational_solved.current_node->stats.row_solution);
    // math::print(rational_solved.current_node->stats.col_solution);
    // // using FloatState = RandomTree<>;
    // // using FloatModel = MonteCarloModel<FloatState>;



    // // FloatState float_state{seed, 1, 2, 2, 1, 0};
    // // FloatModel float_model{0};
    // // TraversedState<FloatModel> float_solved{float_state, float_model};

    // // float_solved.current_node->stats.nash_payoff_matrix.print();


    return 0;
}