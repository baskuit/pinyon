#include <surskit.hh>

/*

Each tree-bandit iteration that does not end on an expanded terminal node will produce a new matrix node.

Thus on an intractibly large game like `MoldState<9>{100}`

*/

int main () {

    // use cart_prod to generate various sessions.

    

    return 0;
}